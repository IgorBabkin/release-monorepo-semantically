# {{lookup releasedVersions pkg.name}} ({{now}})

{{#if (hasBreakingChanges releasedCommits)}}
  ### 💥 BREAKING CHANGES

  {{#each (findBreakingChanges releasedCommits)}}
    - **{{this.scope}}:**
    {{this.subject}}
    {{#if this.hash}}({{this.hash}}){{/if}}
  {{/each}}

{{/if}}
{{#if (hasFeatures releasedCommits)}}
  ### ✨ Features

  {{#each (findFeatures releasedCommits)}}
    - **{{this.scope}}:**
    {{this.subject}}
    {{#if this.hash}}({{this.hash}}){{/if}}
  {{/each}}

{{/if}}
{{#if (hasFixes releasedCommits)}}
  ### 🐞 Bug Fixes

  {{#each (findFixes releasedCommits)}}
    - **{{this.scope}}:**
    {{this.subject}}
    {{#if this.hash}}({{this.hash}}){{/if}}
  {{/each}}

{{/if}}
{{#if (hasPerformance releasedCommits)}}
  ### ⚡ Performance Improvements

  {{#each (findPerformance releasedCommits)}}
    - **{{this.scope}}:**
    {{this.subject}}
    {{#if this.hash}}({{this.hash}}){{/if}}
  {{/each}}

{{/if}}
{{#if (call pkg "hasDependencyUpdates" releasedVersions)}}
  ### 📦 Dependencies

  {{#each (call pkg "getDependencyUpdates" releasedVersions)}}
    - 📦 update
    {{this.packageName}}
    to
    {{this.newVersion}}
  {{/each}}

{{/if}}

{{existing}}
